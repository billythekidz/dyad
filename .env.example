# Dyad Environment Variables Example
# Copy this file to .env and configure as needed

# Use COMPACT system prompt to reduce token usage by ~88%
# FULL prompt: ~5000 tokens (757 lines)
# COMPACT prompt: ~600 tokens (~90 lines)
# Recommended: true (prevents 200k token limit errors)
USE_COMPACT_PROMPT=true
